{
  "name": "grouchy-tag",
  "version": "0.0.0",
  "description": "plugin for hubot (grouchy) to manage project tags throughout deployment process",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ong",
  "license": "ISC",
  "devDependencies": {
    "grunt-cli": "^0.1.13"
  }
}
